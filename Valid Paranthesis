Given a string s containing just the characters '(', ')', '{', '}', '[' and ']', determine if the input string is valid.

An input string is valid if:
Open brackets must be closed by the same type of brackets.
Open brackets must be closed in the correct order.
Every close bracket has a corresponding open bracket of the same type.

Example 1:          Example 2:               Example 3:         Example 4:             Example 5:
Input: s = "()"     Input: s = "()[]{}"      Input: s = "(]"    Input: s = "([])"      Input: s = "([)]"
Output: true        Output: true             Output: false      Output: true           Output: false

A1)
stack = []
pairs = {')': '(', ']': '[', '}': '{'}  
for ch in s:
        if ch in '([{':          
                stack.append(ch)
        elif len(stack)==0:
                return False                
        elif stack[-1]==pairs[ch]:
                stack.pop()
        else:
                return False
return len(stack) == 0

A2)
stack=[]
        pairs = {')': '(', ']': '[', '}': '{'}  
        for ch in s:          
            if ch in '([{':                  (Opening)
                stack.append(ch)
            else:                            (Closing)
                if not stack:                (Closing Case 1: if closing is first elemnt of input)      
                     return False
                if stack[-1] == pairs[ch]:  
                         stack.pop()
                else:
                    return False
        return not stack




Pattern 1: Reject on mismatch (what you already have)
if stack[-1] != pairs[ch]:
    return False
stack.pop()

Pattern 2: Accept on match (what you’re asking for)
if stack[-1] == pairs[ch]:
    stack.pop()
else:
    return False
